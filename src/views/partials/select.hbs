<script>
    $(document).ready(function () {
        var url = window.location;
        console.log(window.location);
        $('div.topnav a[href="' + url + '"]').addClass('active');
        $('div.topnav a').filter(function () {
            return this.href == url;
        }).addClass('active');
    });
</script>